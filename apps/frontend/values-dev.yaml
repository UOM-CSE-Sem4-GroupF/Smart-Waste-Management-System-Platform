# Inherited from base-service
# Inherited from base-service
base-service:
  image:
    repository: ghcr.io/uom-cse-sem4-groupf/dashboard
    tag: latest
  service:
    port: 3000
  livenessProbe:
    httpGet:
      port: 3000
      path: /
  readinessProbe:
    httpGet:
      port: 3000
      path: /
