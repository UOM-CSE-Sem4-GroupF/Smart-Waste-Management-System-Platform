[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: isolated, no cluster required
    component: no cluster required, validates config files statically
    integration: requires running cluster with services deployed
    system: requires full cluster with all services
