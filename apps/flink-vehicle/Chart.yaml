apiVersion: v2
name: flink-vehicle
description: Flink vehicle location processor — writes to InfluxDB vehicle_positions bucket
type: application
version: 1.0.0
dependencies:
  - name: base-service
    version: 1.0.0
    repository: "file://../../helm/charts/base-service"
